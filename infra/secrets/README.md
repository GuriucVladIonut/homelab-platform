# Secrets

This directory must never contain plaintext credentials.

Future permitted files may include encrypted SOPS documents such as:

- `cloudflare.sops.yaml`
- `application.sops.yaml`

Private age key material must remain outside this repository.
