# Flux Bootstrap Boundary

The cluster composition under `infra/cluster` and Flux-compatible resources are prepared. Flux bootstrap is not claimed successful because GitHub authentication and repository ownership are unresolved. Run `flux bootstrap github` only after GITHUB-001 is complete and after reviewing repository permissions.
