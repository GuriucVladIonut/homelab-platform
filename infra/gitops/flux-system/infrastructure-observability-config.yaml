apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-observability-config
  namespace: flux-system
spec:
  interval: 10m0s
  dependsOn:
    - name: infrastructure-observability
  path: ./infra/infrastructure/observability/config
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
