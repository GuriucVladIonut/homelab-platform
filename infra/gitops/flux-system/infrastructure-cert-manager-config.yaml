apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-cert-manager-config
  namespace: flux-system
spec:
  interval: 10m0s
  dependsOn:
    - name: infrastructure-cert-manager
  path: ./infra/infrastructure/cert-manager/config
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
