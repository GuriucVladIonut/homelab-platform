apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: infrastructure-traefik-config
  namespace: flux-system
spec:
  interval: 10m0s
  dependsOn:
    - name: infrastructure-traefik
  path: ./infra/infrastructure/traefik/config
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
