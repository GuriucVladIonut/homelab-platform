apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - issuer-staging.yaml
  - certificate-staging.yaml
  # Add cloudflare-api-token.sops.yaml only after it has been encrypted.
